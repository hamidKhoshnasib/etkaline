import { SupportTicketsView } from "@/features/account/components/SupportTicketsView";

export const metadata = { title: "تیکت‌ها", robots: { index: false, follow: false } };

export default function SupportTicketsPage() {
  return <SupportTicketsView />;
}
