import { SupportView } from "@/features/account/components/SupportView";

export const metadata = { title: "پشتیبانی", robots: { index: false, follow: false } };

export default function SupportPage() {
  return <SupportView />;
}
